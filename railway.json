{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "corepack enable && corepack yarn install --immutable && corepack yarn workspace @founding/nextjs build"
  },
  "deploy": {
    "startCommand": "corepack yarn workspace @founding/nextjs start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
