/**
 * Espejo en JS de los tokens de marca de app/globals.css — necesario
 * donde un valor CSS custom property no llega (config de terceros como
 * Privy, que renderiza su propio iframe/modal). Si cambia el token en
 * globals.css, cambiar acá también; son los dos únicos lugares.
 */
export const BRAND = "#E0FF4F";
export const BRAND_INK = "#00272B";
