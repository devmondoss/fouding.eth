# Pinned to the Rust release used by the Stylus 0.10 toolchain for reproducible WASM builds.

[toolchain]
channel = "1.91.0"
components = ["rust-src", "rustfmt", "clippy"]
targets = ["wasm32-unknown-unknown"]
