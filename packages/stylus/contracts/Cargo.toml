[workspace]
resolver = "2"
members = ["credit-vault", "repayment-router"]
exclude = ["target", "your-contract"]

[profile.release]
codegen-units = 1
strip = true
lto = true
panic = "abort"
opt-level = "z"
